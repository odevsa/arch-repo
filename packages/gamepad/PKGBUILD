pkgname=gamepad
pkgver=0.1.0
pkgrel=1
pkgdesc="Gamepad - A small open-source gamepad tester"
arch=('x86_64')
url="https://github.com/odevsa/gamepad"
license=('none')
provides=("${pkgname}")
conflicts=("${pkgname}")
options=(!strip !debug)

source=("${pkgname}-${pkgver}.deb::https://github.com/odevsa/gamepad/releases/download/v${pkgver}/${pkgname}_${pkgver}_amd64.deb")
sha256sums=('ce115e442afe6a681117ddb7a0ba6c3e0516a127934df3a967deb02608b2de4a')

package() {
  bsdtar -xf data.tar.xz -C "${pkgdir}/"
}
